management:
  endpoint:
    refresh:
      enabled: true
  endpoints:
    web:
      exposure:
        include: refresh,health
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/feature_flags_configuration
    username: root
    password: root
    driverClassName: com.mysql.cj.jdbc.Driver
    referenceUrl: jdbc:mysql://localhost:3306/feature_flags_configuration
    referenceUsername: root
    referencePassword: root
    referenceDriver: com.mysql.cj.jdbc.Driver
#    liquibase:
#      enabled: true
#      change-log: classpath:db/changelog.xml

